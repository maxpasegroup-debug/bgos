/**
 * Shared copy for signup / employee forms (safe for client + server import).
 */
export const EMAIL_ALREADY_IN_USE_MESSAGE =
  "Email already in use. Please use another email." as const;

export const NAME_SIMILARITY_EMAIL_UNIQUE_HINT =
  "Users can have similar names. Email must be unique." as const;
