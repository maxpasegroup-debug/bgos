import { EmployeeRoleDashboard } from "@/components/dashboard/EmployeeRoleDashboard";

export const dynamic = "force-dynamic";

export default function BdmDashboardPage() {
  return <EmployeeRoleDashboard title="BDM dashboard" subtitle="Team BDEs, overrides, and promotion path" />;
}
