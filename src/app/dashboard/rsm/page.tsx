import { EmployeeRoleDashboard } from "@/components/dashboard/EmployeeRoleDashboard";

export const dynamic = "force-dynamic";

export default function RsmDashboardPage() {
  return <EmployeeRoleDashboard title="RSM dashboard" subtitle="Regional performance and BDM network" />;
}
